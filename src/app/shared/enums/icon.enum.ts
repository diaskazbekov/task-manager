import { RESTORE_D } from "../constants/svg-path-d.constant";

export enum EIcon {
  SEARCH = 'search',
  MAIL = 'mail',
  STAR = 'star',
  CHECK = 'check',
  TRASH = 'trash',
  VERTICAL_DOTS = 'vertical_dots',
  WARNING = 'warning',
  X = 'x',
  RESTORE = 'restore'
}
